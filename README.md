# IntelliDocs SaaS

- `backend/` contains the FastAPI API and the preserved Python document/RAG engine.
- `frontend/` is reserved for the future React/Vite/TypeScript client.

See `backend/README.md` for local setup and Supabase requirements.
